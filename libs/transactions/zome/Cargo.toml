[package]
edition = "2018"
name = "hc_lib_transactions"
version = "0.0.1"

[lib]
crate-type = ["cdylib", "rlib"]
name = "hc_lib_transactions"

[dependencies]
serde = "1"
derive_more = "0"
chrono = {version = "0.4", features = ["serde"]}

hdk = { version = "0.0.127", features = ["encoding"] }
